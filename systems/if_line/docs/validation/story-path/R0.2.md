# R0.2 Validation

- Item: define the StoryPath authoring and publication architecture.
- Expected commit: `docs(architecture): define story path and publication model`
- Author and committer: `fbh <2697927157@qq.com>`

## Checks

```text
$ git diff --check
exit 0

$ for section in 'Target Entity Model' 'State Machines' \
    'Publication and Draft Truth Table' 'Revision Versus Branch Examples' \
    'Architecture Acceptance Invariants'; do rg -q ...; done
exit 0

$ for term in StoryPath ChapterSlot StoryPathChapter ProjectPublication \
    CandidateSetRevision ChapterScriptRevision VNGraphRevision \
    context_manifest superseded withdrawn changes_pending; do rg -q ...; done
exit 0

$ test <R0.1-and-R0.2-complete-and-R0.3-pending>
exit 0
```

The document defines the target ER model, ownership and dependency direction,
artifact/path/release state machines, publication truth table, frozen context
contract, transaction boundaries, examples, migration boundary, and acceptance
invariants. This item changes documentation only, so runtime suites are not
required.
