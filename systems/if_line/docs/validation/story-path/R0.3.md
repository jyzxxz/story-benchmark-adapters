# R0.3 Validation

- Item: freeze the replacement OpenAPI contract and legacy route mapping.
- Expected commit: `docs(api): freeze replacement authoring api contract`
- Author and committer: `fbh <2697927157@qq.com>`

## Checks

```text
$ git diff --check
exit 0

$ python <parse-yaml-and-validate-local-refs>
paths=43 operations=58 refs=valid

$ python <assert-required-and-removed-path-sets>
required paths present; removed paths absent

$ for section in 'Backend Review' 'Frontend Review' \
    'Data Migration Review' 'Review Decision'; do rg -q ...; done
exit 0

$ test <R0.1-through-R0.3-complete>
exit 0
```

The OpenAPI 3.1 contract parses successfully, has unique operation IDs, resolves
all local references, declares every path variable, contains the required UUID
and atomic-publication routes, and excludes the removed index-based routes. The
migration document records backend, frontend, and data review outcomes.
