# R1.4 Validation

- Item: key Script and VNGraph Heads by immutable source revisions.
- Expected commit: `refactor(model): bind scripts and graphs to immutable revisions`
- Author and committer: `fbh <2697927157@qq.com>`

## Checks

```text
$ backend/venv/bin/python -m pytest \
    backend/tests/test_revision_keyed_artifact_heads.py \
    backend/tests/test_chapter_script_pipeline.py \
    backend/tests/test_v2_vn_graphs.py \
    backend/tests/test_visual_asset_workflows.py \
    backend/tests/test_v2_branch_generation.py -q
44 passed

$ backend/venv/bin/python -m pytest \
    backend/tests/test_runtime_foundation.py::test_revision_keyed_heads_preserve_legacy_pointers \
    backend/tests/test_runtime_foundation.py::test_alembic_upgrades_empty_sqlite_database \
    backend/tests/test_runtime_foundation.py::test_v2_migration_schema_and_constraints_are_fingerprint_locked \
    backend/tests/test_runtime_foundation.py::test_v2_migration_table_order_is_postgresql_fk_safe \
    backend/tests/test_runtime_foundation.py::test_postgresql_offline_ddl_renders_complete_fk_safe_chain \
    backend/tests/test_runtime_foundation.py::test_v2_migrations_downgrade_to_foundation_and_upgrade_again -q
6 passed

$ backend/venv/bin/python -m pytest backend/tests/test_v2_releases.py -q \
    -k 'not release_manifest_is_immutable_idempotent_and_authorizes_storage'
8 passed, 1 deselected

$ backend/venv/bin/python -m pytest \
    backend/tests/test_project_release_lifecycle_guard.py -q \
    -k 'not v2_project_creation_is_always_private_draft and not v2_publication_creates_release_and_withdrawal_precedes_delete'
2 passed, 2 deselected

$ cd backend
$ APP_ENV=test DATABASE_URL=postgresql://user:pass@localhost/db PYTHONPATH=. \
    venv/bin/python -m alembic -c alembic.ini \
    downgrade 0025_revision_keyed_artifact_heads:0024_candidate_set_versioning --sql
exit 0

$ git diff --check
exit 0

$ backend/venv/bin/python -m py_compile <changed-python-files>
exit 0
```

The migration assigns stable UUID primary keys without changing old Head
pointers, keeps legacy project/index columns as non-unique lookup indexes, and
adds nullable unique source Revision keys for the R7.2 backfill. This permits
sibling paths to own distinct Heads at the same display index. Both exact
source foreign keys use `ON DELETE CASCADE`; current Revision pointers remain
restricted.

The broader affected regression run passed 59 tests and exposed four failures
that reproduce individually outside Head behavior: one obsolete draft-release
status expectation, two obsolete `is_draft` response-field expectations, and
one existing usage-event query failure. The release contract mismatches are in
the R1.5/R5 scope; the unrelated usage query is not changed by this item.
