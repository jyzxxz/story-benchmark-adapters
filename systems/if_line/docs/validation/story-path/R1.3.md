# R1.3 Validation

- Item: add versioned CandidateSet revisions, Heads, and path provenance.
- Expected commit: `feat(model): version branch candidate sets`
- Author and committer: `fbh <2697927157@qq.com>`

## Checks

```text
$ backend/venv/bin/python -m pytest \
    backend/tests/test_candidate_set_models.py \
    backend/tests/test_v2_branch_generation.py \
    backend/tests/test_v2_branching.py -q
19 passed

$ backend/venv/bin/python -m pytest \
    backend/tests/test_runtime_foundation.py::test_candidate_set_versioning_preserves_legacy_candidates \
    backend/tests/test_runtime_foundation.py::test_alembic_upgrades_empty_sqlite_database \
    backend/tests/test_runtime_foundation.py::test_v2_migration_schema_and_constraints_are_fingerprint_locked \
    backend/tests/test_runtime_foundation.py::test_v2_migration_table_order_is_postgresql_fk_safe \
    backend/tests/test_runtime_foundation.py::test_postgresql_offline_ddl_renders_complete_fk_safe_chain \
    backend/tests/test_runtime_foundation.py::test_v2_migrations_downgrade_to_foundation_and_upgrade_again -q
6 passed

$ git diff --check
exit 0

$ backend/venv/bin/python -m py_compile <changed-python-files>
exit 0
```

CandidateSet revisions are immutable and scoped by StoryPath plus checkpoint.
Different revisions at the same checkpoint may reuse option keys, while keys
remain unique inside one set. The nullable bridge preserves legacy candidates
for the explicit R7.2 backfill. The migration also passes empty-schema, offline
PostgreSQL, schema fingerprint, and full downgrade/upgrade validation.
