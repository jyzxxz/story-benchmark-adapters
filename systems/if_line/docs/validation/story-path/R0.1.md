# R0.1 Validation

- Item: merge the StoryPath execution blueprint into the repository authority.
- Expected commit: `docs(blueprint): define story path refactor execution plan`
- Author and committer: `fbh <2697927157@qq.com>`

## Checks

```text
$ git diff --check
exit 0

$ test "$(... | rg -c '^- \[[ x_]\] \*\*R[0-8]\.[0-9]+\*\*')" -eq 41
exit 0

$ test -z "$(... | rg -o '\*\*R[0-8]\.[0-9]+\*\*' | sort | uniq -d)"
exit 0

$ test "$(... | rg -c '^- \[x\] \*\*R0\.1\*\*')" -eq 1
exit 0
```

The StoryPath section contains 41 unique work items, preserves the completed
legacy P1-P4 history, and marks only R0.1 complete. This documentation-only
item does not change runtime code, so no application test suite is required.
