# R1.1 Validation

- Item: add StoryPath, ChapterSlot, and StoryPathChapter identity models.
- Expected commit: `feat(model): add story path chapter identity model`
- Author and committer: `fbh <2697927157@qq.com>`

## Checks

```text
$ backend/venv/bin/python -m pytest backend/tests/test_story_path_models.py -q
6 passed

$ backend/venv/bin/python -m pytest \
    backend/tests/test_runtime_foundation.py::test_alembic_upgrades_empty_sqlite_database \
    backend/tests/test_runtime_foundation.py::test_v2_migration_schema_and_constraints_are_fingerprint_locked \
    backend/tests/test_runtime_foundation.py::test_v2_migration_table_order_is_postgresql_fk_safe \
    backend/tests/test_runtime_foundation.py::test_postgresql_offline_ddl_renders_complete_fk_safe_chain \
    backend/tests/test_runtime_foundation.py::test_v2_migrations_downgrade_to_foundation_and_upgrade_again -q
5 passed

$ backend/venv/bin/python -m pytest \
    backend/tests/test_v2_tasks.py backend/tests/test_v2_revisions.py -q
15 passed

$ git diff --check
exit 0

$ backend/venv/bin/python -m py_compile <changed-python-files>
exit 0
```

The full `test_runtime_foundation.py` run was also attempted: 16 tests passed
and two unrelated runtime fixture tests failed. One metrics test queries the
global database without creating `generation_tasks`; one readiness test expects
a Redis check that the current settings projection does not enable. All five
migration/schema tests affected by R1.1 pass independently, including offline
PostgreSQL DDL and a complete SQLite downgrade/upgrade cycle.
