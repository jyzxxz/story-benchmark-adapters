# 2026-09-07 并行重测：更换密钥与 IF Line HTML 修复后

这是当前代码的验证记录。此前的 [初始图文验证](BATCH_VALIDATION.md) 与旧密钥复测分别保留，不把不同版本、不同样本或不同时点的结果合并为一次成功。

## 本轮条件与源码范围

三个根运行于 2026-09-07 11:04 UTC 起同时执行；每项目 `count=1, concurrency=1`。每个项目内部的多故事并发另由 `count=2, concurrency=2` 的本地固定响应测试验证。真实测试没有重复派发同一个根运行，也没有在原生失败后自动重跑。

- 公共案例：`CAMPUS-01-V4`，2421 字符的同一份 `shared_task.txt`。SHA-256：`d2fb88452de72a51743f18cae9b62ef1e21a70c4f28d74a8a7ba1a0f2f3e5ce5`。
- 固定开头 SHA-256：`196d04a58ba5bce543e93788e496ae42fdd547d1583d185cda42c3b969159c65`。
- 文字：`deepseek-v4-flash`，共同 `thinking.type=disabled`；图片：`gpt-image-2`；原生视觉审核：`gpt-5.4-mini`。模型角色和参数按共同配置冻结，原生流程和输出可以不同。
- 一条实际路径，固定选项策略 `[0]`，达到 4000 个新增可见 Unicode 字符后延伸到共同句界。该窗口仍是开发候选，不强制全篇结局，也不因有无结局加减分。
- 适配器不限制总请求、总 token、生成时间或费用。原生校验、审核、重试、预取以及供应商限制仍然生效。
- IF Line 使用用户授权的 [HTML 转换修复变体](IFLINE_HTML_PATCH.md)，1202 个文件按原始锁与独立补丁检查。AI4VisualNovel 35 个、InfiPlot 927 个文件保持原始冻结版本。
- IF 源码补丁 SHA-256：`76e44a1d6ee1044620fc65b2bfb6a7b93c4eed2b7ebc44d5305d4087ebc5c79d`。原始 `baseline-lock.json` 未覆盖；计划冻结实际源树及补丁哈希，运行前后核对。

## 真实生成结果

三个任务均已停止并封存。3020 + 94 + 869 = 3983 个证据文件通过哈希与指标重算检查；三个真实 worker 的运行区间确实重叠。接收端输入判等、首次创作请求检查、运行前后源码身份和执行代码冻结检查通过。`comparison_ready=true` 表示成功与失败样本均可供分析，不表示三个项目均完成故事。

| 项目 | 新增可见字符 | 实际执行选择 | clean/UI 画面对数 | 当前结果 |
| --- | ---: | ---: | ---: | --- |
| IF Line + HTML 修复 | 4015 | 2 | 104 | 达到共同阅读窗口 |
| AI4VisualNovel | 0 | 0 | 0 | 原生故事图生成 13 个节点，原生校验要求 12 个，停止 |
| InfiPlot | 4021 | 3 | 75 | 达到共同阅读窗口 |

IF Line 实际生成两个新增章节并转换为脚本，104 段展示正文和 104 对画面都有关联证据。两章均没有 HTML 标签，因此本轮证明修复版本的真实生成、转换和播放流程工作；具体 HTML 故障的证明来自下述三份旧真实失败响应回放，不能把新样本说成触发了相同 HTML 特例。本轮 81 次模型尝试中，80 次 HTTP 200；一次资产流程中的文字请求发生 TLS EOF，原生后续调用继续成功，用量未知被保留。没有再出现 HTML 标签与可见正文不一致导致的校验错误。第二章第一次分段漏掉正文，严格覆盖校验仍将其拒绝，原生重试后通过；该事件与 HTML 无关，没有人工补写正文或放宽校验。

AI4VisualNovel 的 8 次请求全部 HTTP 200，用量完整，没有本轮 401、429 或预算截断。4 次规划、3 次审核和 1 次故事图请求后，`agents/designer_agent.py` 的节点数检查拒绝 `root + node1…node12`。原生源码和该校验均未修改。这个结果证明鉴权和接入已通过，不能证明该项目本轮成功产出故事；也不能仅凭一个样本推断公共提示词完全不会影响原生生成错误的概率。

InfiPlot 的 75 段实际正文均关联到确切 Writer 操作，75 对画面均能关联正文修订、资产与供应商候选。原 10 MiB 以上的三个约 12.84 MB 请求被原生服务接受并进入文字与图片生成，外置容量实际为 64 MiB。达到阅读窗口时客户端结束了三个预取响应，它们没有完整返回体，不能按服务器预设状态值宣称 HTTP 200 完整交付；后续图像调用已经收尾并保存候选和 usage。全局响应采集记为不完整，实际展示的 75 段正文来源完整。

InfiPlot 另有一次 Writer 上游 `IncompleteRead`，原生继续处理。该失败调用不在当前展示正文的来源链上；保留失败与未知用量，不推断已展示文字被降级，也不把缺失用量记为零。

## 八项指标保留范围

| 指标 | 本轮可核验的证据与边界 |
| --- | --- |
| M1 固定事实与要求满足率 | 公共任务、brief、固定开头、时间解释、带原文坐标的要求条目、实际正文、选择和停止原因；未调用质量裁判。 |
| M2 故事连贯性 | 按实际路径排列的正文、来源修订、选择及调用来源；不拼接互斥分支。IF 的调用 ID 按原生脚本任务关联，包含任务内失败尝试和视觉指导，不冒称每个 ID 都直接生成了最终正文；独立审计另按任务的章节与脚本引用核对实际分段请求。 |
| M3 人物视觉一致性 | 人物版本、参考资产、原生资产及实际显示画面的对应关系；素材缺少或未出场情况保留。 |
| M4 等待时间 | 原始事件、观察器、时钟域和阶段时长；本地离屏观测不冒充用户桌面实际等待，无法同钟测量的项为 null。 |
| M5 阅读体验 | 匿名正文包包含公共开头、实际路径、选择和统一窗口规则；不向裁判暴露模型、成本或内部大纲。 |
| M6 图文匹配 | 每段正文对应 clean 画面、带 UI 画面、修订 ID 和图片资产；不拿生图 prompt 代替实际正文评分。 |
| M7 token 消耗 | 全部供应商调用、用途、重试、原始 usage 及已知小计；调用用量缺失时总量为 null，并报告覆盖。 |
| M8 图片生成数量 | 请求次数、供应商返回候选、原生资产、派生资产和实际显示使用分别记录，共享资产不重复计数。 |

AI4VisualNovel 没有正文或画面，五项内容质量不能评分；失败耗时和真实 token 仍可统计；图片调用确实为零。所有质量分数保持 `null`，评审包标记 `prepared_not_scored`。数据结构齐全、文件封存及来源校验通过，都不等于内容质量通过。

IF Line 文字调用 47 次，其中 46 次有用量，已知小计 154400 tokens，完整总量未知；25 次图片调用共 31360 图像服务 tokens，9 次视觉审核调用共 22669 tokens。25 个图片候选均保存，另有 11 个派生资产；36 个原生/派生资产中 14 个用于实际路径，不能将 104 对画面当成 104 次生图。

InfiPlot 文字调用 21 次，其中 20 次有用量，已知小计 106051 tokens，总量未知；11 次图片调用均有用量，共 44013 图像服务 tokens，11 个返回候选全部保留。AI4VisualNovel 文字 39892 tokens（输入 30069、输出 9823），缓存命中是输入中的子项，不重复相加。文本、图像、视觉审核分列；实际账单没有读取，费用字段不冒充实际金额。

## 修复与本地验证

- IF HTML 修复：96 项原生回归通过。上一轮三份真实失败响应全部重现旧版的标签覆盖错误，修复版均生成 74 段，2951 个原始字符和 2870 个可见非空白字符保持不变；12 个正文删改、调序等负例全部拒绝，4 个实体/比较符边界通过。
- 公共记录 23 项、历史 v3 返回 38 项通过。补丁与证据保存 20 项、旧来源校验 3 项通过。测试组有包含关系，不相加宣称互不重复的总数。
- InfiPlot 响应旁录 15 项通过，含真实 112 MiB HTTP 流与 Chromium 缓存驱逐；原生 Next/React 本地流程 7 项通过。采集不改变原生响应字节、回调或背压。
- 本轮 InfiPlot 两个本地 worker 实际重叠执行，各自生成 74 字、4 对画面、1 次选择，各有 203 个证据文件通过校验；恢复运行新增调用为 0。全部 34 次调用仅到本地固定响应服务。此前 IF Line、AI4VisualNovel 的双 worker 检查保留为各自对应版本的工程证据，不称为本次三个项目各两份真实长故事。
- 独立使用检查验证四份配置模板均能加载、引用路径存在，三入口、数量/并发控制、密钥环境变量、依赖及评分边界均有说明。这不是新机器从零安装验证。

本轮执行代码对应提交 `dfe81070aceb75e9a46ecd824f004cd92e6e1de9`；后续提交只补验收文档。可机读的核验摘要及三个本地证据档案 SHA-256 见 [验证摘要](validation/parallel-recheck-20260907.json)。

## 使用与交付

安装、三个启动命令、调整 `count` / `concurrency`、恢复与比较见 [批量运行说明](BATCH_RUNNING.md)。记录文件和八项指标字段见 [记录说明](BATCH_RECORDING.md)，InfiPlot 大响应旁录见 [响应记录说明](INFIPLOT_RESPONSE_RECORDING.md)。

真实故事、原始图片和逐次调用证据保存为独立本地档案，原始密钥不提交。仓库发布代码、补丁、使用说明与核验摘要；旧失败档案保留，供分析失败率和成本，不能从统计中删除。
